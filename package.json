{
  "name": "express_server",
  "version": "1.0.1",
  "description": "api for WeeFit",
  "main": "server/index.ts",
  "scripts": {
    "server": "node server/index.ts",
    "build": "rm -rf build/ && prettier --write source/ && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nathaniel James",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^10.0.0",
    "ejs": "^3.1.6",
    "express": "^4.17.1",
    "ffmpeg": "^0.0.4",
    "ffprobe": "^1.1.2",
    "localenv": "^0.2.2",
    "routing-controllers": "^0.9.0",
    "ts-node": "^10.2.0",
    "typescript": "^4.3.5"
  },
  "devDependencies": {
    "@types/ejs": "^3.0.7",
    "@types/express": "^4.17.13",
    "@types/ffmpeg": "^1.0.4",
    "@types/ffprobe": "^1.1.2"
  }
}
